import React from 'react';
const Card = (users) => {
    return (
        <div>
            <h2>hello</h2>
            {/* {users.map((element, index) => {
                return(
                    <div key={index}>{element}</div>
                )
            })} */}
        </div>
    )
}
export default Card;